name: Run Command on Stateful Generator
run-name: "Remote exec: ${{ inputs.command }}"

on:
  push:
    branches:
      - stateful-runner-interaction
  workflow_dispatch:
    inputs:
      command:
        description: "Shell command to execute"
        required: true
        type: string
      timeout:
        description: "Timeout in minutes"
        required: false
        type: number
        default: 30

jobs:
  exec:
    runs-on: stateful-generator
    timeout-minutes: ${{ fromJson(inputs.timeout || '720') }}
    steps:
      - name: Download and extract jochemnet snapshot
        if: ${{ inputs.command == '' }}
        run: |
          set -e
          URL="https://snapshots.ethpandaops.io/jochemnet/nethermind/24402727/snapshot.tar.zst"
          DEST="/mnt/sda/jochemnet/nethermind"
          ARCHIVE="/mnt/sda/jochemnet/snapshot.tar.zst"

          rm -rf /mnt/sda/jochemnet
          mkdir -p "$DEST"

          echo "Downloading snapshot (resumable)..."
          until curl -fL -C - --retry 10 --retry-delay 30 --retry-max-time 3600 -o "$ARCHIVE" "$URL"; do
            echo "Download interrupted, retrying in 60s..."
            sleep 60
          done

          echo "Download complete. Size: $(du -h "$ARCHIVE" | cut -f1)"
          echo "Extracting..."
          cd "$DEST"
          zstd -d --threads=0 "$ARCHIVE" --stdout | tar xf -

          echo "Removing archive..."
          rm -f "$ARCHIVE"

          echo "Done. Contents:"
          ls -la "$DEST"

      - name: Execute command
        if: ${{ inputs.command != '' }}
        run: |
          echo "::group::Command"
          echo "${{ inputs.command }}"
          echo "::endgroup::"
          echo "::group::Output"
          eval "${{ inputs.command }}"
          echo "::endgroup::"
